IMPORTANT - THIS VERSION FIXES index.html ITSELF

You were correct: the previous access screen was implemented mainly in Android MainActivity,
so opening index.html directly could bypass/not show that screen.

THIS VERSION:
- index.html itself shows the ₹50/month Premium Access screen immediately.
- Tap "Get Access / Subscribe ₹50".
- It asks for Mobile Number + Owner Access Code.
- It verifies the SAME signed codes generated by your existing Owner Access Generator.
- Valid code => complete billing app opens.
- Invalid/expired code => app remains locked.
- Saved valid code is checked again when index.html opens.

TEST:
Open the root index.html in this ZIP after extracting it. You should see the access screen first.
